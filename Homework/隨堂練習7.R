pi
ceiling(pi)
floor(pi)
round(pi, digits = 2)

today_char <- "Today is:"
sys_date <- Sys.Date()
paste(today_char, sys_date)

num_vector <- c(11:14, NA)
sum(num_vector, na.rm = TRUE)
median(num_vector, na.rm = TRUE)